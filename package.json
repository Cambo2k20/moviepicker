{
  "name": "discordians-cine-cord",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/verify-dist.mjs",
    "preview": "vite preview",
    "test": "npm run test:unit && npm run test:e2e",
    "test:unit": "node --test tests/roulette-core.test.js tests/auth-core.test.js",
    "test:e2e": "playwright test",
    "git:cleanup": "node scripts/git-cleanup.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "2.110.8"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "vite": "^7.1.3"
  }
}
